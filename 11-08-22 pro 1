int subtractProductAndSum(int n)
{
    int product=1,sum=0;
   // scanf("%d",&n);
    while(n>0)
    {
        int temp=n%10;
        product=product*temp;
        sum=sum+temp;
        n=n/10;
    }
    return product-sum;

}
